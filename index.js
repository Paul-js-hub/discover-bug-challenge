//function takes an array returns a square of its elements
// fix the function so that it returns correct output
let detectBug = (array) =>{
    let res = arra.map(arr => ar * 2);
}

console.log(detectBug([1, 2, 3, 4]))